import { CriticalTraceNote } from './CriticalTraceNote.js'
export class FakeCriticalTraceNote extends CriticalTraceNote {
    hasInput = false
    updateSequential() {
        //none
    }
    updateParallel() {
        super.updateParallel()
        if (time.now >= this.targetTime) {
            this.despawn = true
            return
        }
    }
    touch() {
        //none
    }
    terminate() {
        //none
    }
}
